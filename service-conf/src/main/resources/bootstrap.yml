spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/Jonat/Desktop/CloudProject/config-data
  security:
    user:
      name: ${USER}
      password: ${PASS}
server:
  port: 9000